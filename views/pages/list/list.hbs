<!DOCTYPE html>
<html lang="en">
    {{> pagehead}}

    <body>
        {{> nav}}

        {{#unless user}}
            {{!-- If user is not logged in they get sent back to the home page --}}

            {{!-- uncomment this later --}}
            {{!-- <script> window.location.replace("/") </script> --}}
        {{/unless}}

        <div id="container" class="fl-col new-user">
        {{#if user}} {{!--This is a temporary if to block out this code --}}
            {{!-- Add a page view for a new user with no saved recipes --}}
            <h1>No recipes found.</h1>
            <span></span>
            <h2>Click the button to create your first recipe!</h2>
            <div id="add-button">
                <p class="button-text">Add New Recipe</p>
                <div class="button-frame">
                    <span class="line"></span>
                    <span class="line"></span>
                </div>
            </div>
            <span></span>
            <img src="/assets/images/arrows.png" alt="arrows" id="arrows" draggable="false">
        {{/if}}

            {{!-- Add a page view for a user with saved recipes --}}
            {{!-- This view should pull from the database and use the info to display recipe cards --}}


            {{!-- Add a page view for creating new recipes --}}
            {{!-- This view should be similar to viewing saved recipes, but requier a URL or user input to fill out the data --}}

            {{!-- Form for selecting URL or Create New Recipe --}}
            <div></div>

            {{!-- Form for creating new recipes --}}
            <div id="nr-container" class="fl-col">
                <div id="nr-he">
                    {{!-- Header area, Title, Picture(?), Maybe some other info --}}

                </div>

                <div id="nr-in" class="fl-col">
                    {{!-- List of ingredients --}}
                    <div class="flex">
                        <h2>Ingredients</h2>
                        <span class="nr-in-sp"></span>
                        <div class="fl-col">
                            <p>Prep Time</p>
                            <img src="/assets/images/add_circle-24px.svg" alt="">
                        </div>
                        <span class="nr-in-sp"></span>
                        <div class="fl-col">
                            <p>Cook Time</p>
                            <img src="/assets/images/add_circle-24px.svg" alt="">
                        </div>
                        <span class="nr-in-sp"></span>
                    </div>
                    <hr>
                    <div id="nr-in-li" class="flex"> {{!-- Maybe add a conditional: if < 6 items: load vertical list. else if > 10 items: load list in rows of 3. else: load in rows of 2--}}
                        <img src="/assets/images/add_circle-24px.svg" alt="">
                    </div>
                </div>

                <div id="nr-di" class="fl-col">
                    {{!-- Recipe directions --}}
                    <h2>Directions</h2>
                    <hr>
                    <div id="nr-di-li" class="fl-col-fl">
                        <img src="/assets/images/add_circle-24px.svg" alt="">
                        {{!-- <hr> --}}
                    </div>

                </div>

                <div id="nr-no" class="fl-col">
                    {{!-- Writers notes --}}
                    <hr>
                    <h3>Notes</h3>
                    {{!-- Reveal this when the user clicks "add notes" --}}
                    <div id="nr-no-ta" class="fl-col">
                        <div class="fl-col">
                            <h3>Add Notes</h3>
                            <img src="/assets/images/add_circle-24px.svg" alt="">
                        </div>
                        <textarea name="notes" cols="20" rows="5" style="display: none;"></textarea>
                    </div>
                    <hr>

                </div>

                <div id="nr-nu"> {{!-- Probably move this to the "View saved" page and leave it out of creating --}}
                    {{!-- Nutrition info, no user input here --}}
                    <h3>Nutrition Info</h3>

                </div>
            </div>

            {{!-- Add a page view for viewing saved recipes --}}
            
            {{!-- This view should pull from the database to fill out the data --}}
        </div>

        {{> footer}}
    </body>

    <script id="new-template" type="text/x-handlebards-template"></script>
    
</html>